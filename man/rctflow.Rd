% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rctflow.R
\name{rctflow}
\alias{rctflow}
\title{rctflow diagram}
\source{
Example dataset: `RCT_tDCS`
}
\usage{
rctflow(data, group_col, group_labels = NULL)
}
\arguments{
\item{data}{A data frame with one row per participant that includes at least:
- `subject`: unique participant identifier (no `NA` for randomized subjects).
- a grouping variable (e.g. `Group`) that indicates randomization to
  active vs sham/control.}

\item{col_name}{Optional character string used in the diagram (for example,
as a dataset label or title; e.g., `"RCT_tDCS"`).}
}
\value{
A `DiagrammeR` htmlwidget object containing the flowchart.
}
\description{
a package that creates a flowchart describing RCT data, with one control and one sham group using `DiagrammeR`
}
\details{
data obtained from kaggle opensourcedata
}
\examples{
\dontrun{
library(DiagrammeR)

# Example: create a flowchart from RCT_tDCS
rctflow(
  data    = RCT_tDCS,
  group_col = "Group"
)
}
}
\author{
Jil Soni, Lamya Serhir
}
